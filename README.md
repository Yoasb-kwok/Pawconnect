# Too Cute To Handle

Pet matching & community platform (肉球不滿).

## Web app

The app lives in the **`web/`** folder.

```bash
cd web
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). See `web/docs/DEMO-ACCOUNTS.md` for demo logins.
